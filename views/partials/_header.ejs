<header>
  <!--added a header partial-->
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav mr-auto">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>

      <%if (!user) {%>
        <!--added a login button-->
        <form method="POST" action="/login">
          <input type="text" name="username">
          <button type="submit" class="btn btn-primary m-3"><b>Log in</button></b>
        </form>

        <%} else {%>
          <!--added a Username and logout button-->
          <b>
            <%=`Logged in as: ${user.email}`%>
          </b>
          <form method="POST" action='/logout'>
            <button type="submit" class="btn btn-danger m-3"> <b>Log Out</button></b>
          </form>

          <% } %>
    </div>
  </nav>
</header>